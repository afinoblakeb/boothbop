// App-wide constants.

/** Number of photos captured per booth session. */
export const SHOTS = 4;

/** The horizontal transparent brand logo, base-path aware. */
export const LOGO = `${import.meta.env.BASE_URL}watermark.png`;
