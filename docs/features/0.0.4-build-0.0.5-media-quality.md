# BoothBop 0.0.4 Build 0.0.5: Quality Foundation

## Release

- Candidate version: App Store `0.0.4`, replacement build `0.0.5`
- Owner: BoothBop
- Decision date: 2026-07-21
- Target evaluation date: Not scheduled; quality gates control readiness
- Public production baseline: `0.0.3 (0.0.3)` at `0e9497c`
- Release-line source baseline: `0.0.4 (0.0.4)` at `b287baf`
- Candidate branch: `codex/0.0.4-build-0.0.5-media-quality`

## Consumer Problem

Creating, editing, reopening, and sharing a BoothBop session can feel slow and
can silently reduce the quality of the photos the user just captured.

## Evidence

- User feedback: First GIF sharing feels like the app has stalled; Low output is
  visibly unacceptable; shared video looks materially softer than the strip.
- Pre-change observation: The inherited path built a 7.9-megapixel preview on
  the UI thread, JPEG-encoded native video frames before H.264, stored gallery
  frames at JPEG quality 0.85, and reopened every saved frame at 720px.
- Frequency or severity: These defects touch the core capture-to-share journey
  and affect every session, not an edge feature.
- Why this problem matters now: Social sharing is the principal completion
  action and the product promises private, beautiful media created on device.

## Proposed Capability

Replace the current ad-hoc media work with one high-quality render pipeline.
BoothBop always captures and exports at its best supported quality, uses small
screen previews, preserves source dimensions in My Photos, deduplicates and
prewarms share renders, keeps progress feedback responsive, and encodes native
video without an intermediate lossy image format.

The approved quality sweep also makes the iOS experience native-first: launch
proceeds directly to camera, `AVCapturePhotoOutput` supplies full-resolution
front-camera stills with continuous focus/exposure/white balance, camera
navigation exposes Camera, My Photos, and Settings, My Photos has its own
one-tap Camera action, gallery deletion follows a Select-then-Delete flow, and
the classic strip is an exact 2x6 print ratio with four square photo windows. A
shared future-retro design system modernizes the visible shell while preserving
BoothBop's cream brand canvas.

This is a quality correction, not a new creative feature.

## Simpler Alternatives Considered

1. Increase only the video bitrate. Rejected because it cannot recover pixels
   already lost to JPEG, gallery downscaling, or low-resolution capture.
2. Add only a loading spinner. Rejected because synchronous work freezes the
   spinner and leaves the destructive media pipeline unchanged.

## Non-Goals

- New templates, props, captions, accounts, analytics, subscriptions, or social
  network SDKs.
- Direct publishing to Instagram or destination-specific authentication.
- New edit tools or a broad creative-feature release.

## User Journey

The user captures four photos and reaches review without a post-capture stall.
The visible strip and editor use a screen-sized preview. Selecting GIF starts
preparing the Instagram-safe MP4 once in the background while the toolbar shows
a responsive preparation state. A prepared result is reused instead of
starting a duplicate encode. On web, the first tap may prepare a high-quality
asset and the next tap opens the share sheet so browser user-activation rules
are preserved. Saved sessions reopen from lossless original-dimension masters.

## Required States

- First use: Best quality is automatic; no quality configuration is required.
- Repeat use: Prepared media is reused until an output-affecting choice changes.
- Loading: Visible, responsive, accessible progress appears within 100ms.
- Empty: Export actions remain unavailable before four valid frames exist.
- Error: Preparation failure is actionable and the original GIF remains usable.
- Permission denied: Capture and Photos failures preserve the existing recovery.
- Offline: Capture, rendering, gallery, saving, and sharing remain on device.
- Compact phone: Preview, progress, and actions remain visible at 320x568.
- Large phone: Preview uses available space without rendering excess pixels.
- Accessibility: Busy state is announced and controls expose disabled/readiness
  state without relying on animation or color.

## Success Criteria

- Behavioral: One keyed job serves preview, autosave, save, and share callers;
  stale work cannot replace current media.
- Quality: Gallery round-trips preserve source dimensions; native video receives
  lossless frame inputs; H.264 uses High Profile at 1080x1920 for social export;
  no legacy Low or Standard preference can reduce a new export.
- Capture: A real iPhone prefers the local `AVCapturePhotoOutput` bridge and
  falls back to WebKit only when no native camera device is available; preview
  and saved selfies use exactly one mirror operation.
- Print: The complete classic output, including borders and footer, is exactly
  1:3 at preview and export sizes, with four square photo windows.
- Performance: Screen preview cell size is at most 360px; filter interaction
  produces no task over 100ms in the production browser journey; share feedback
  appears within 100ms; a prewarmed native share opens without a new encode.
- TestFlight target: App Store-upgrade install shares a sharp Post, Reel, and
  Story; first Share never appears frozen; reopened sessions match fresh output.

## Removal Criteria

Hold the replacement if it lowers decoded media fidelity, adds a sustained
black/blank state, exceeds the performance budgets, loses a gallery session, or
causes the social MP4 to fail Instagram import.

## Test Plan

- Unit coverage: Quality migration, source-size preservation, render-job
  deduplication/invalidation, profile values, social geometry, frame ordering.
- Browser journey: Long-task budgets, responsive busy state, prewarm reuse,
  editor latency, full-resolution export dimensions, gallery round-trip.
- Native simulator: Clean and update launch, capture review, background prewarm,
  share-sheet presentation, cancellation, compact/standard/large layouts.
- Media inspection: Decode PNG/GIF/MP4, compare dimensions and representative
  pixels, verify H.264 profile/container/duration/no audio and feed-safe geometry.
- Physical update install: Install build 0.0.5 through TestFlight over 0.0.3 and
  share to Instagram Post, Reel, Story, Messages, and Save Video.
- Airplane Mode: Complete capture, edit, gallery reopen, and every export.

## Current Verification Evidence

Verified on 2026-07-21 from commits `aa2cd12` and earlier on the candidate
branch:

- `npm run check` passes typecheck, ESLint with zero warnings, Prettier check,
  21 Vitest files, and 146 unit tests.
- Unit coverage includes quality migration, render-job cancellation and
  invalidation, gallery schema migration and source-dimension preservation,
  native-share cleanup, Photos permission handling, native-video contracts,
  native-camera activation/lifecycle, direct-camera launch recognition, and
  exact 2x6 square-cell strip geometry.
- `npm run check:e2e` passes all 23 production-browser journeys in 1.1 minutes,
  including cold native-shell launch, capture single-flight, compact through
  large layouts, editor responsiveness, GIF-to-MP4 sharing, fallback behavior,
  Retake One, branding persistence, and remote feature disabling.
- The Release simulator target compiles for arm64 and x86_64 with zero warnings.
- `npm run ios:smoke` passes fresh installs on iPhone 17e, iPhone 17, and iPhone
  17 Pro Max simulators plus an overwrite-update install on iPhone 17. Every
  launch recorded zero black transition frames and emitted no matched native
  launch failures.
- Native bridge tracing proved that Capacitor registers and dispatches to the
  local camera plugin. Simulator lacks an `AVCaptureDevice`, so it intentionally
  exercises the tested WebKit fallback; physical-camera ISP behavior remains an
  owner-review item.

Remaining before owner approval:

- Create the signed `0.0.4 (0.0.5)` archive, export the IPA, and pass Apple's
  server-side validation.
- Install on the owner's iPhone and review physical-camera quality plus the
  Instagram handoff.
- No upload, TestFlight install, active-submission replacement, or App Review
  submission is permitted before owner approval.

## Prototype Reuse

- `src/lib/videoNative.ts` and `BoothBopVideo` are corrected, not replaced by a
  prototype branch.
- `src/lib/session.ts`, `src/lib/socialShare.ts`, and the 0.0.4 crop contract are
  retained.
- No wholesale merge from a prototype branch is permitted.

## Release Decision

- [ ] Ship
- [ ] Revise
- [ ] Hold

Decision notes: Ready for signed archive and owner review. Source, browser,
Release compile, and simulator launch/update gates pass. Keep the decision open
until archive validation and the owner's iPhone review are complete; do not
upload or replace the active App Store submission before that approval.
